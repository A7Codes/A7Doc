Public Class Pat

    Public name As String
    Public phone As String
    Public address As String

    Public Sub New(ByVal name, ByVal phone, ByVal address)
        Me.name = name
        Me.phone = phone
        Me.address = address
    End Sub

End Class
